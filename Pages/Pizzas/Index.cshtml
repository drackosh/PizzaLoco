@page
@model PizzaLoco.Pages.Pizzas.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Pizzas</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Pizza[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pizza[0].Ingredients)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pizza[0].Price) (30 cm)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Pizza) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ingredients)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price) RON
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.PizzaId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.PizzaId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.PizzaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
